# RepoImmune v0.1.0 — The repository remembers

RepoImmune's first public alpha turns a real fixed bug into executable memory and blocks its deliberate return.

Highlights:

- Behavior Card Schema v1 with JSON Schema, TypeScript types, evidence levels and deterministic content hashes.
- Python AST patch immunity, fixed-form removal and regression-assertion weakening checks.
- CLI, read-only GitHub Action, SARIF/Markdown/HTML, MCP server and open Agent Skill.
- Real Astropy #12907 evidence chain and dependency-free offline behavioral surrogate.
- Static no-login demo plus reproducible SWE-bench Verified snapshot/experiment pipeline.

Honest boundary: most dataset-scale cards are externally reported; 30 generated capsules are structural patch replays, not full upstream executions. Independent mining-precision labeling and equal-budget Agent A/B trials remain future work.

